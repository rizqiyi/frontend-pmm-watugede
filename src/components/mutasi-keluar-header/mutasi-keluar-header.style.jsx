import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  textPengusul: {
    fontWeight: 400,
  },
  paperPengikut: {
    width: "auto",
  },
  textDetail: {
    fontSize: 14,
    fontWeight: 450,
    cursor: "pointer",
  },
}));
